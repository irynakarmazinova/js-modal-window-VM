// это база к которой буду подключать плагины
const $ = {};

// объект $ по ум складывается в глобальный объект window
// модульности нет - js все складывает в глобальный скоуп
// можно явно не указывать
// window.$ = $;
